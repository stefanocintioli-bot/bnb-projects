# BNB Chain DCA Agent — Python Dependencies

# Blockchain interaction
web3==6.20.3

# AI decision engine — pinned to avoid httpx proxies kwarg conflict
groq==0.9.0
httpx==0.27.0

# Telegram notifications (direct HTTP — no persistent bot needed)
requests==2.32.3

# Environment variable loading
python-dotenv==1.0.1
